import { useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { Mail, Lock, Eye, EyeOff, GraduationCap, Building2, Landmark, Loader2, ArrowRight, Sparkles, ShieldCheck } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

const ROLES = [
  { key: 'student', label: 'Student', icon: GraduationCap, tagline: 'Find internships that match your real skills.', metric: 'Build your career signal' },
  { key: 'industry', label: 'Industry', icon: Building2, tagline: 'Discover skill-verified, ready-to-hire talent.', metric: 'Hire with confidence' },
  { key: 'institution', label: 'Institution', icon: Landmark, tagline: 'See skill-gap analytics across your college.', metric: 'Act on market intelligence' },
];

export default function Login() {
  const [role, setRole] = useState('student');
  const [email, setEmail] = useState(''); const [password, setPassword] = useState('');
  const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  const { login } = useAuth(); const navigate = useNavigate(); const active = ROLES.find((item) => item.key === role);
  const submit = async (event) => { event.preventDefault(); setError(''); setLoading(true); try { const user = await login(email, password); navigate(user.role === 'industry' ? '/industry' : user.role === 'institution' ? '/institution' : '/'); } catch (err) { setError(err.response?.data?.message || 'Login failed. Check your email and password.'); } finally { setLoading(false); } };
  return <div className="min-h-screen relative overflow-hidden bg-[#071B36] text-white flex items-center justify-center p-4 sm:p-7">
    <div className="absolute inset-0 opacity-80" style={{ backgroundImage: 'radial-gradient(circle at 12% 15%, #0C78CB 0, transparent 30%), radial-gradient(circle at 82% 16%, #7337B7 0, transparent 24%), radial-gradient(circle at 55% 92%, #E77732 0, transparent 30%)' }} />
    <div className="absolute inset-0 opacity-[.16]" style={{ backgroundImage: 'linear-gradient(rgba(255,255,255,.3) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.3) 1px, transparent 1px)', backgroundSize: '52px 52px' }} />
    <main className="login-shell relative z-10 w-full max-w-6xl overflow-hidden rounded-3xl border border-white/25 shadow-2xl flex flex-col lg:flex-row">
      <section className="p-7 sm:p-10 lg:p-12 flex flex-col justify-between bg-white/[.10] backdrop-blur-xl border-b lg:border-b-0 lg:border-r border-white/15"><div><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-2xl bg-white/15 border border-white/20 grid place-items-center font-black text-xl text-orange-200">A</div><div><p className="font-bold text-xl tracking-tight">AYUSH-SETU</p><p className="text-[10px] tracking-[.22em] text-blue-100">SKILL INTELLIGENCE</p></div></div><div className="mt-14 lg:mt-20 max-w-xl"><div className="inline-flex items-center gap-2 text-xs font-semibold tracking-wider text-orange-100 bg-orange-300/10 border border-orange-100/20 rounded-full px-3 py-1.5"><Sparkles size={14} /> ONE CONNECTED CAREER ECOSYSTEM</div><h1 className="mt-5 text-4xl sm:text-5xl font-bold leading-[1.07] tracking-tight">Potential, made <span className="text-transparent bg-clip-text bg-gradient-to-r from-orange-200 to-yellow-100">visible.</span></h1><p className="text-blue-50/80 text-base sm:text-lg leading-7 mt-5">AYUSH-SETU connects student capability, industry demand, and institutional action through transparent skill intelligence.</p></div></div><div className="mt-10"><div className="grid sm:grid-cols-3 gap-3">{ROLES.map((item) => { const Icon = item.icon; const chosen = role === item.key; return <button type="button" key={item.key} onClick={() => setRole(item.key)} className={`text-left rounded-2xl p-4 border transition-all duration-300 ${chosen ? 'bg-white/20 border-white/50 shadow-lg -translate-y-1' : 'bg-white/[.06] border-white/10 hover:bg-white/[.12]'}`}><Icon size={20} className={chosen ? 'text-orange-200' : 'text-blue-100'} /><p className="text-sm font-semibold mt-3">{item.label}</p><p className="text-[11px] text-blue-100/70 mt-1 leading-4">{item.metric}</p></button>; })}</div><div className="mt-5 flex items-center gap-2 text-xs text-blue-100/70"><ShieldCheck size={15} className="text-emerald-300" /> Explainable matching. Your data stays in your control.</div></div></section>
      <section className="bg-white/[.93] backdrop-blur-2xl text-navy flex items-center p-7 sm:p-10 lg:p-12"><div className="w-full max-w-sm mx-auto"><div className="lg:hidden flex items-center gap-2 mb-10"><div className="w-9 h-9 bg-navy text-white rounded-xl grid place-items-center font-black">A</div><span className="font-bold">AYUSH-SETU</span></div><p className="text-xs font-bold tracking-[.18em] text-brand">WELCOME BACK</p><h2 className="text-3xl font-bold tracking-tight mt-2">Continue your journey.</h2><p className="text-gray-500 text-sm mt-2 mb-8">{active.tagline}</p>{error && <div role="alert" className="bg-red-50 border border-red-200 text-red-700 text-sm rounded-xl px-4 py-3 mb-5">{error}</div>}<form onSubmit={submit} className="space-y-5"><div><label className="text-sm font-semibold text-gray-700">Email address</label><div className="relative mt-2"><Mail size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-brand" /><input type="email" placeholder="you@example.com" value={email} onChange={(event) => setEmail(event.target.value)} required className="w-full bg-white/75 border border-slate-200 rounded-xl pl-11 pr-4 py-3.5 focus:ring-2 focus:ring-brand/40 focus:border-brand outline-none transition" /></div></div><div><div className="flex justify-between"><label className="text-sm font-semibold text-gray-700">Password</label><span className="text-xs text-brand">Secure sign-in</span></div><div className="relative mt-2"><Lock size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-brand" /><input type={showPassword ? 'text' : 'password'} placeholder="Enter your password" value={password} onChange={(event) => setPassword(event.target.value)} required className="w-full bg-white/75 border border-slate-200 rounded-xl pl-11 pr-12 py-3.5 focus:ring-2 focus:ring-brand/40 focus:border-brand outline-none transition" /><button type="button" aria-label={showPassword ? 'Hide password' : 'Show password'} onClick={() => setShowPassword((value) => !value)} className="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 hover:text-navy">{showPassword ? <EyeOff size={19} /> : <Eye size={19} />}</button></div></div><button disabled={loading} className="w-full bg-gradient-to-r from-brand to-[#156FBC] hover:brightness-110 text-white py-3.5 rounded-xl font-semibold shadow-lg shadow-blue-500/20 transition flex items-center justify-center gap-2 disabled:opacity-60">{loading ? <><Loader2 size={18} className="animate-spin" /> Logging in...</> : <>Log in securely <ArrowRight size={18} /></>}</button></form><p className="text-sm mt-7 text-center text-gray-500">New to AYUSH-SETU? <Link to="/register" className="text-brand font-bold hover:underline">Create an account</Link></p><div className="mt-7 rounded-xl bg-slate-50 border border-slate-100 px-4 py-3"><p className="text-[11px] text-gray-400 text-center leading-5"><strong className="text-gray-500">Demo access</strong><br />student1@demo.com · industry1@demo.com · institution1@demo.com<br />Password: password123</p></div></div></section>
    </main>
  </div>;
}
